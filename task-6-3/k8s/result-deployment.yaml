apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: result
  name: result
spec:
  replicas: 1
  selector:
    matchLabels:
      app: result
  template:
    metadata:
      labels:
        app: result
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
      containers:
      - image: dockersamples/examplevotingapp_result:lastest
        name: result
        ports:
        - containerPort: 80
          name: result
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
            ephemeral-storage: "50Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
            ephemeral-storage: "100Mi"
        securityContext:
          readOnlyRootFilesystem: true
        imagePullPolicy: Always
